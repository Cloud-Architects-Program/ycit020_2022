### Add your values
gcp_project_id = "$SEEDING" # Seeding Projet
gcp_region = "us-central1"
billing_account = "$BILLING" #gcloud beta billing accounts list
project_name    = "$STUDENT_NAME-notepad-dev" # New Project
bucket_name   = "$STUDENT_NAME-notepad-dev-tfstate" # TF State for new Project
